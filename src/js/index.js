import '../scss/styles.scss'
import { selectId } from './helpers'
import Fetch from './components/Fetch'

console.log('index.js') // add custom select

if (selectId('listContainer')) {
  /* eslint-disable-next-line */
  let data = new Fetch()
}
